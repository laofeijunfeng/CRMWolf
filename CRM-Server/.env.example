DATABASE_URL=mysql+pymysql://root:N0k2x4gzjnbbH3zEmDPW@localhost:3306/crm_db
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 前端应用地址（用于飞书通知跳转链接）
FRONTEND_URL=https://crm.example.com
PUBLIC_API_BASE_URL=https://crm.example.com

# Agent Query 语义解析单次超时（秒）
AGENT_QUERY_SEMANTIC_TIMEOUT=30

# Agent Root 路由结构化请求单次超时（秒）
AGENT_ROOT_DECISION_TIMEOUT=30

# 出站 AI 请求默认不读取本机 HTTP(S) 代理；需要代理时显式填写
AI_HTTP_TRUST_ENV=false
AI_HTTP_PROXY=
AI_HTTP_TIMEOUT_SECONDS=60
